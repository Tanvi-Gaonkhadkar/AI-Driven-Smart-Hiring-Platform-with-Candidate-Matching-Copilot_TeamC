streamlit==1.38.0
pandas==2.2.2
plotly==5.24.0
google-genai>=1.0.0
python-dotenv==1.0.1
pdfplumber==0.11.4
python-docx==1.1.2
requests==2.32.3

# AI Document Verification (image-based documents) - also requires the
# Tesseract OCR system binary installed separately; see utils/ocr.py.
# The app runs fine without it, image verification just falls back to
# "manual review needed" if Tesseract isn't found.
pytesseract==0.3.13
Pillow==10.4.0
